export type {
  ButtonEventHandler,
  ButtonSubmission,
  SubmitProps,
  CancelProps
} from 'components/Core/FormProps';

export interface InputProps {
  name: string;
  users: number[]; // array of  user ids
}
